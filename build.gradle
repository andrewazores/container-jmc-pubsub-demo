plugins {
    id 'java'
    id 'application'
}

repositories {
    jcenter()
}

dependencies {
}

jar {
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
    exclude 'META-INF/*.RSA', 'META-INF/*.SF','META-INF/*.DSA'
}

mainClassName = "es.andrewazor.containertest.publisher.Publisher"
